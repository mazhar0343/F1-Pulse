# Frontend Environment Variables (for Vercel)
# Set this in Vercel dashboard under Project Settings → Environment Variables

# Backend API URL
# Replace with your deployed backend URL (e.g., https://f1-pulse-backend.railway.app)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend Environment Variables (for Railway/Render)
# Set these in your backend platform's environment variables

# Allowed CORS origins (comma-separated)
# Include your Vercel domain and localhost for development
# Example: https://your-app.vercel.app,https://*.vercel.app,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001

